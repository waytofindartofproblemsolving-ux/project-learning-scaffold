interface:
  display_name: "Agent Eval Skill"
  short_description: "Design focused agent evals"
  default_prompt: "Use $agent-eval-designer only when eval scenarios or pass/fail criteria are needed."
