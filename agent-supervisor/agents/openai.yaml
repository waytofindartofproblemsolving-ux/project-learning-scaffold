interface:
  display_name: "Agent Supervisor Skill"
  short_description: "Compact oversight for agent work"
  default_prompt: "Use $agent-supervisor for one compact supervision pass over this agent task."
