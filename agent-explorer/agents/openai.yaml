interface:
  display_name: "Agent Explorer Skill"
  short_description: "Investigate one unknown agent fact"
  default_prompt: "Use $agent-explorer only for the specific docs, APIs, or code paths that are unknown."
